package dameo.test;

import dameo.move.Move;
import dameo.move.MultiPieceMove;
import java.util.Set;

/**
 * Class with scrap code that I don't want to throw out just yet.
 * @author Wim
 */
public class Junk {
//        Set<Move> multiMoves = super.generateSingleMoves(s);
//        for (Move m : multiMoves) {
//            MultiPieceMove mul = (MultiPieceMove) m;
//            if (mul.getMoves().size() > 1) {
//                moves.add(m);
//            }
//        }

//              From negamax
//            // This player has no more moves: this negamax player loses
//            if (color == negamaxColor) {
//                bestMove = new Edge(null, Integer.MIN_VALUE);
//            }
//            // Opponent has no more moves: this negamax player wins
//            else {
//                bestMove = new Edge(null, Integer.MAX_VALUE);
//            }
}
